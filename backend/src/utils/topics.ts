/**
 * Рубрикатор (тематические категории) — используется и для специализации
 * наставника (users.specialization), и для темы события (events.topic).
 * Простой varchar на обеих таблицах (не Postgres enum) — список можно менять,
 * не трогая схему БД, но на границе (DTO) валидируем по этому списку, чтобы
 * значения совпадали с тем, что реально показывается и фильтруется на сайте.
 */
export const TOPICS = [
  'Кино',
  'Музыка',
  'Искусство',
  'Литература',
  'Театр',
  'Здоровье',
  'Новые увлечения',
] as const;

export type Topic = (typeof TOPICS)[number];
